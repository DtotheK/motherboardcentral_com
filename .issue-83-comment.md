Blocked on #82. Escalating to `needs-human` rather than opening a red PR.

## The blocker

This issue's third acceptance criterion is:

> `scripts/ftpm-advisory.test.mjs` is updated to select by socket (the `grep` above) rather than by filename, and its count assertion is raised to 38.

That file does not exist on `main`. It was introduced by PR #82, which is still **open** and labelled `needs-human` (35 files, over the 15-file tripwire in CLAUDE.md, so it was correctly not auto-merged).

On `main` today, exactly **one** review page carries the advisory block — `review-msi-mag-b550-tomahawk.html`, from #76:

```
$ grep -lE 'CVE-2026-6726' review-*.html
review-msi-mag-b550-tomahawk.html
```

So a branch cut from `main` cannot satisfy this issue. Adding the block to the three pages here would bring the total to 4, and a test asserting 38 would fail. There is no ordering of the work on `main` that ends green.

## What I did verify

Everything else in the issue is accurate against `main` at 61077c9:

- The socket selector returns 38 pages:
  ```
  $ grep -lE '<td>Socket</td>[[:space:]]*<td>AM[45]' review-*.html | wc -l
  38
  ```
- The delta versus the #20 filename filter is exactly the three pages named, no more and no fewer:
  ```
  $ grep -lE '<td>Socket</td>[[:space:]]*<td>AM[45]' review-*.html \
      | grep -Eiv 'b550|x570|a620|b650|x670|x870'
  review-asus-rog-crosshair-viii-dark-hero.html
  review-gigabyte-b450m-d3hp.html
  review-gigabyte-b450m-d3hp-wifi6e.html
  ```
- None of the three currently mentions the advisory (`grep -c 'CVE-2026-6726'` → 0 on all three).
- Each has exactly one `          <h2 id="specifications">Full Specifications</h2>` line, so the insertion anchor is unambiguous.
- The source block is at `review-msi-mag-b550-tomahawk.html` lines 137–143, followed by one blank line before the heading — matching the placement this issue specifies.

The content work itself is genuinely three files and mechanically trivial. Only the sequencing is wrong.

## To unblock

Merge #82 first. Once it lands, `main` has the 35 pages plus `scripts/ftpm-advisory.test.mjs`, and this issue becomes a clean 4-file change (3 pages + the test) that merges normally, exactly as the issue anticipates.

I did not stack a branch on top of `task/issue-20` instead. That would have grown the diff a human is already reviewing from 35 files to 38, and auto-merge would have targeted `task/issue-20` rather than `main` — a scope change to someone else's open review, which is not mine to make. Happy to take this in a follow-up run the moment #82 is in.

No PR opened. No files changed.
