PR #79 is up and labelled `needs-human`. Auto-merge is not enabled, because the page does not follow one part of the content spec above.

**The CPU spec in this issue is wrong.** The issue says:

> the 9800X3D is a 65 W-class TDP part with locked-down X3D behaviour

Published specifications say **120W default TDP**, and the 9800X3D is **unlocked for overclocking** — it is the first X3D part that is, because the second-generation V-Cache die sits under the cores rather than on top of them.

- Newegg product page, fetched 2026-08-20: 8-Core / 16-Threads, 8MB L2, 96MB L3, 120W TDP, Socket AM5, "unlocked for overclocking"
- Walmart listing: "8-Core - 16-Thread 4.7 GHz (5.2 GHz Max Boost) Socket AM5 **Unlocked** Desktop Processor"
- Tom's Hardware review of the part

Neither `amd.com` product page nor `shop-us-en.amd.com` would answer an automated fetch, so per CLAUDE.md rule 1 I am flagging rather than treating this as settled. The sources above agree with each other; the only thing they disagree with is this issue.

The page states 120W and unlocked, and a test pins both figures. Everything else in the framing survives unchanged — a 120W part is still an easy load for any current AM5 board, so the "VRM capacity matters less here than on a 9950X" argument is exactly what the page makes.

Two smaller notes:

- The sibling pages' `Budget Guide` section is a table of unsourced price bands. The acceptance criteria here forbid fabricated prices, so this page has a price-free `Chipset Guide` in the same slot instead.
- `js/main.js` holds the nav search index and all 11 existing per-CPU guides have an entry. This page does not, so it will not surface in site search. Adding it means editing shared JS, which this issue does not specify — left for a follow-up.

Four files changed. `npm run validate` passes with 0 new violations and an unchanged baseline; all 330 tracked tests pass.
