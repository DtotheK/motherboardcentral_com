## Result: 4 UNVERIFIED, 0 conversions — no PR opened

This is the outcome the issue anticipated ("the likely result is **four UNVERIFIED boards and zero conversions**"). Following the issue's own instruction for that branch: findings below, `needs-human` applied, no PR, nothing committed.

### Step 2 — measured file list re-verified (branch cut from `main` @ `61077c9`)

`grep -rn "s?k=" --include=*.html .` for this batch's boards returns exactly 4 hits, all in one file — the measured list in the issue is correct, no drift since `09ed961`:

| Line | URL |
|---|---|
| `best-motherboard-for-i9-9900k.html:108` | `s?k=GIGABYTE+Z390+AORUS+Ultra+motherboard` |
| `best-motherboard-for-i9-9900k.html:119` | `s?k=ASUS+ROG+Maximus+XI+Hero+WiFi+motherboard` |
| `best-motherboard-for-i9-9900k.html:130` | `s?k=MSI+MPG+Z390+Gaming+Edge+WiFi+motherboard` |
| `best-motherboard-for-i9-9900k.html:141` | `s?k=ASRock+Z390+Taichi+motherboard` |

One URL spelling per board, one occurrence each. No other file references any of the four.

### Step 3 — no spec table exists for any of the four boards

Confirmed three ways:

- **No review page.** `ls | grep -iE 'z390|maximus|9900'` returns only `best-motherboard-for-i9-9900k.html` plus `review-asus-rog-maximus-z790-hero.html` and `review-asus-rog-maximus-z890-hero.html` — different boards (Z790 / Z890, not Maximus XI / Z390).
- **No database record.** `grep -rn -i 'z390' --include=*.js .` returns a single hit, `js/main.js:578`, which is the guide's own `description` string — not a `motherboardDatabase` entry.
- **No spec rows on the guide page.** Each board carries only a marketing blurb. The complete spec data we publish for all four:

| Board | Everything we state |
|---|---|
| GIGABYTE Z390 AORUS Ultra | 12+1 phases · direct-touch heatpipe · WiFi · 3x M.2 · Intel GbE |
| ASUS ROG Maximus XI Hero WiFi | "Premium VRM" · AI Overclocking · Thunderbolt 3 · WiFi |
| MSI MPG Z390 Gaming Edge WiFi | Extended heatsinks · WiFi · RGB |
| ASRock Z390 Taichi | 12-phase VRM · dual GbE · WiFi · 3x M.2 |

No socket row, no chipset row, no memory-generation row for any board.

### Why all four are UNVERIFIED

Verification test **(c)** is *"socket, chipset and memory generation on that page agree with our spec table."* There is no spec table for any of these four boards, so (c) has no reference to evaluate against and cannot be satisfied by any candidate ASIN. The issue is explicit: *"If test (c) cannot be evaluated against a real spec table, the board is UNVERIFIED... Do not lower the bar to 'the model name has Z390 in it'."*

This is structural, not a search failure — no ASIN could pass, so no `WebSearch`/`WebFetch` was run. All four links left untouched.

| Board | Status | Reason |
|---|---|---|
| GIGABYTE Z390 AORUS Ultra | **UNVERIFIED** | No spec table on site — test (c) unevaluable |
| ASUS ROG Maximus XI Hero WiFi | **UNVERIFIED** | No spec table on site — test (c) unevaluable |
| MSI MPG Z390 Gaming Edge WiFi | **UNVERIFIED** | No spec table on site — test (c) unevaluable |
| ASRock Z390 Taichi | **UNVERIFIED** | No spec table on site — test (c) unevaluable |

PENDING-HUMAN (spec disagreement): none — no fetch reached that stage.

### Repo state

No files changed, nothing committed, branch `task/issue-55` discarded. Specifically:

- `validation-baseline.json` `count` stays at **98**; all four entries remain (lines 20–23, rule `affiliate-search-url`).
- `docs/affiliate-asins.md` unchanged — none of the four boards was in the ledger and none was added.
- Repo-wide `tag=motherboardcentral.com-20` count unchanged at **341**.

### Consequence for #4

The baseline does **not** reach the floor described in this issue. These 4 `affiliate-search-url` entries are permanently baselined unless a human takes one of these paths:

1. **Write review pages** for one or more of the four boards, giving test (c) a spec table to check against; this batch can then be re-run.
2. **Refresh the i9-9900K picks** to current-availability boards that already have review pages. The issue puts this out of scope here and asks for a separate issue — flagging rather than filing, since it is a content/recommendation decision.
3. **Accept** the four search URLs as a permanent exception for discontinued 2018-era boards.

All four belong in the rollup issue that closes out #4.
