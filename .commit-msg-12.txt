Stream job logs live and write a readable summary alongside them

run-job.sh buffered the whole run into one .json via ">" redirection, so a
job in flight could not be tailed and the output was raw JSON only.

- switch to --output-format stream-json --verbose and pipe through tee, so
  the log grows line by line and tail -f shows activity while a job runs
- extract the result event to a sibling .txt with jq, using -R + fromjson?
  so interleaved stderr noise cannot break extraction
- echo starting/finished lines naming the job and the time
- capture the pipeline status so a failing job still produces its .txt and
  finished line, then propagate the real exit code

Adds jobs/run-job.test.mjs, which drives the real script in a temp dir with
fake git and claude binaries on PATH.

Closes #12

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
