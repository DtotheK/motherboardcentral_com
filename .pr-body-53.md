Closes #53
Part of #4

Converts the A620 batch's Amazon search URLs to direct `/dp/<ASIN>` product
links. **1 of 4 boards converted.** Three boards failed verification and keep
their search URLs untouched.

## Board → ASIN

| Board | ASIN | Status |
|---|---|---|
| MSI PRO A620M-E | `B0BZW9RG3P` | ✅ converted |
| ASRock A620M-HDV/M.2+ | — | ⚠️ UNVERIFIED |
| ASUS Prime A620M-A | — | ⚠️ UNVERIFIED |
| GIGABYTE A620M DS3H | — | ⚠️ UNVERIFIED |

## Changed pages

- https://motherboardcentral.com/review-msi-pro-a620m-e.html
- https://motherboardcentral.com/reviews.html

Plus `validation-baseline.json` and `docs/affiliate-asins.md` — **4 files total.**

## Verification evidence — MSI PRO A620M-E → `B0BZW9RG3P`

All four "confidently verified" tests passed:

- **(a) Exact model-name match.** Our `<h1>` is `MSI PRO A620M-E`. Fetched title:
  `MSI PRO A620M-E Motherboard, Micro-ATX - Supports AMD Ryzen 7000 Series Processors, AM5 - DDR5 Memory Boost 6400+MHz/OC, 1 x PCIe 4.0 x16, 1 x M.2 Gen4`.
  Token-by-token match; not `PRO A620M-B`.
- **(b) Live fetch.** `https://www.amazon.com/dp/B0BZW9RG3P` returned a real product page.
- **(c) Specs agree.** Listing says AM5 / A620 / DDR5 / Micro-ATX. Our spec table
  says Socket `AM5`, Chipset `A620`, Memory `DDR5, 2 DIMM slots, up to 64GB`,
  Form Factor `Micro-ATX`. No discrepancy. No spec table was edited.
- **(d) New and bare.** Standard new listing — not Renewed, not Refurbished, not
  a bundle or kit.

Candidates rejected before fetching: three `Fit for MSI PRO A620M-E …` knockoffs
(`B0G4PZ9TWS`, `B0GDQQGWJT`) including junk-brand prefixes `THFKLXT`
(`B0FR5HRJPC`) and `CJWDPDSWEZ` (`B0G6KF4CKH`), plus a `parts-quick` RAM listing
and `Pro A620M-C EX`.

## UNVERIFIED boards (links left untouched)

| Board | Why |
|---|---|
| **ASRock A620M-HDV/M.2+** | `B0C1ZZDZTJ` matches the model exactly (`ASROCK A620M-HDV/M.2+ Supports AMD Socket AM5 Ryzen 7000 Series Processors`), but test (b) failed: bare `/dp/` returned **HTTP 500**, and the one allowed retry on the long SEO URL also returned **HTTP 500**. No other genuine listing exists — remaining search hits were `A620AM-X`, `A620AM-HVS`, `A620M Pro RS` (different boards) and a "Fit for" knockoff. |
| **ASUS Prime A620M-A** | No candidate survived the reject list, so no fetch was attempted. Amazon carries `Prime A620M-A-CSM` (`B0C3ZLCSVP`) and `Prime A620M-K CSM` (`B0CT58F5GQ`) — both carry an extra `-CSM` SKU token, and the `-K` is a different board. Re-queried without `site:` per the recipe; same outcome. |
| **GIGABYTE A620M DS3H** | `B0CD2KY2G7` matches the model exactly (`Gigabyte A620M DS3H Motherboard - Supports AMD Ryzen 8000 CPUs …`), but test (b) failed: bare `/dp/` **HTTP 500**, long SEO URL retry **HTTP 500**. No other genuine listing — remaining hits were `A620M Gaming X`, `A620M H`, `A620M S2H`, `A620M C`, and a Renewed listing. |

**PENDING-HUMAN (spec discrepancies): none.** No candidate reached test (c) and
disagreed.

The three HTTP 500s are Amazon-side and look transient. These boards can be
retried in a follow-up run without any rework here.

## Acceptance criteria

- [x] **Zero `s?k=` hits for every converted board.** `grep -rn "s?k=MSI+PRO+A620M-E" --include=*.html .` → 0 hits. The three UNVERIFIED boards above retain theirs, by design.
- [x] **URL format.** All 4 new occurrences are exactly `https://www.amazon.com/dp/B0BZW9RG3P?tag=motherboardcentral.com-20`, matching `^https://www\.amazon\.com/dp/[A-Z0-9]{10}\?tag=motherboardcentral\.com-20$`.
- [x] **Tag count invariant.** `grep -ro "tag=motherboardcentral.com-20" . --include=*.html | wc -l` → **336 before, 336 after.**
- [x] **`npm test` exits 0** — 38 pass, 0 fail. **`npm run validate` exits 0** with `NEW = 0` and `RESOLVED = 0` after the baseline refresh.
- [x] **Baseline drops by exactly 2** — `count` 159 → 157, matching the 2 (page, URL) pairs converted (`review-msi-pro-a620m-e.html` and `reviews.html`). All 157 remaining entries are `affiliate-search-url`; no other rule appears.
- [x] **`git diff -U0` is href-only.** 4 changed lines, all inside `href="…amazon…"`. `rel`, `target`, `class`, `style` and link text are byte-identical. No card markup, nav, footer, layout or prose touched. Plus `validation-baseline.json` and `docs/affiliate-asins.md`.
- [x] **Ledger uniqueness.** `B0BZW9RG3P` appears exactly once in `docs/affiliate-asins.md`, mapped to exactly one board, and was not previously present.

## Merge gate

Per the issue's step 12, auto-merge requires **zero UNVERIFIED**. This PR has
three, so it is labelled `needs-human` and auto-merge is **not** enabled.

🤖 Generated with [Claude Code](https://claude.com/claude-code)
