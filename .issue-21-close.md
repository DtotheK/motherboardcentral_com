Verified: microcode 20260811 / IPU 2026.3 does not place desktop Raptor Lake (LGA1700) or Arrow Lake-S (LGA1851) in any affected-products list. ARL-S is absent from the 20260811 platform table entirely; RPL-S receives a functional revision bump only (0x133 to 0x137). Per this issue's own stop condition, no content change. Advisory triage table is in the planner comment above. Reopen only if Intel publishes a per-advisory list naming desktop 700/800-series platforms.

Backlog-worker close-out checks, run 2026-08-20:

- Re-attempted caution C1 (INTEL-SA-01371 / INTEL-SA-01372 affected-version tables). Both `intel.com/.../intel-sa-0137{1,2}.html` still return **HTTP 403** to automated fetch, so the CSME/SPS branch remains unverified against a primary source. Disposition is unchanged: unverified means add nothing (CLAUDE.md rule 1). C1 still deserves five minutes from a human with a browser; if the tables name client CSME on 700/800-series, reopen with chipset-firmware scope.
- `git status --porcelain` (tracked): empty. No `task/issue-21` branch exists, and none was created.
- `npm test`: 74 passed, 0 failed.
- `npm run validate`: `NEW (fail build) : 0`, `RESOLVED : 0`, `PASSED: no new violations.` Baseline reads 159 known rather than the 181 recorded at plan time — merged affiliate batches (#59 and earlier) resolved the difference; no baseline update was made or needed on this issue.

No branch, no PR, no page edits, per D8.
