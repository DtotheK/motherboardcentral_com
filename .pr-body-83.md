Closes #83

Issue #20 scoped the AMD fTPM advisory by **filename**
(`b550|x570|a620|b650|x670|x870`), which matched 35 pages. Three more review
pages describe Socket AM4/AM5 boards but carry no chipset token in their
filename, so they got nothing. This adds the advisory to those three and
switches the test to select by socket.

Unblocked by #82, which merged as `d356f19`. That was the only thing holding
this issue up — the earlier run correctly deferred rather than stacking on an
open review.

## Changed pages

- https://motherboardcentral.com/review-asus-rog-crosshair-viii-dark-hero.html
- https://motherboardcentral.com/review-gigabyte-b450m-d3hp.html
- https://motherboardcentral.com/review-gigabyte-b450m-d3hp-wifi6e.html

Plus `scripts/ftpm-advisory.test.mjs`. **4 files — under the 15-file tripwire,
no shared template, nav, footer or layout markup touched.**

## Acceptance criteria — evidence

**The approved block appears exactly once on each of the three pages**

```
$ grep -c 'id="ftpm-advisory"' review-asus-rog-crosshair-viii-dark-hero.html review-gigabyte-b450m-d3hp.html review-gigabyte-b450m-d3hp-wifi6e.html
review-asus-rog-crosshair-viii-dark-hero.html:1
review-gigabyte-b450m-d3hp.html:1
review-gigabyte-b450m-d3hp-wifi6e.html:1

$ grep -lE 'CVE-2026-6726' review-*.html | wc -l
38
```

**Byte-identical to the other 35 apart from the vendor `href` and link text**

Hashing the block with only the vendor anchor normalized out returns one hash
for the three new pages and for an existing ASUS and GIGABYTE page:

```
8ab9035ad028836611d803145f92d62f  review-asus-rog-crosshair-viii-dark-hero.html  (new)
8ab9035ad028836611d803145f92d62f  review-gigabyte-b450m-d3hp.html               (new)
8ab9035ad028836611d803145f92d62f  review-gigabyte-b450m-d3hp-wifi6e.html        (new)
8ab9035ad028836611d803145f92d62f  review-asus-rog-crosshair-x670e-hero.html     (existing)
8ab9035ad028836611d803145f92d62f  review-gigabyte-a620m-ds3h.html               (existing)
```

Across all 38 pages, each fixed line has exactly **one** distinct variant
(`sort -u | wc -l` → 1 for the AMD-SB-7064 paragraph, the BitLocker paragraph,
the date stamp, and the vendor paragraph with the vendor normalized).

**The test selects by socket and asserts 38**

```diff
-/* Same selector the issue specifies: every AM4/AM5 review page by filename. */
+const SOCKET_ROW = /<td>Socket<\/td>\s*<td>AM[45]/;
 const PAGES = fs
   .readdirSync(ROOT)
   .filter((f) => /^review-.*\.html$/.test(f))
-  .filter((f) => /b550|x570|a620|b650|x670|x870/i.test(f))
+  .filter((f) => SOCKET_ROW.test(fs.readFileSync(path.join(ROOT, f), 'utf8')))

-test('the AMD advisory scope covers at least the 35 pages issue #20 names', () => {
-  assert.ok(PAGES.length >= 35, ...);
+test('the AMD advisory scope covers all 38 AM4/AM5 review pages', () => {
+  assert.ok(PAGES.length >= 38, ...);
```

**Each page links only its own brand's vendor page**

```
review-asus-rog-crosshair-viii-dark-hero.html: https://www.asus.com/security-advisory/
review-gigabyte-b450m-d3hp.html:               https://www.gigabyte.com/Support/Security
review-gigabyte-b450m-d3hp-wifi6e.html:        https://www.gigabyte.com/Support/Security
```

The existing test `each advisory links its own vendor and no other` also asserts
no page links any other vendor, and now runs over all 38 pages.

**No AGESA version, no socket token, no fixed-BIOS claim**

```
$ grep -A7 'id="ftpm-advisory"' <the 3 pages> | grep -ciE 'AGESA|ComboAM|\bAM[45]\b'
0
```

The copy says "we have not verified that a fixed BIOS exists for this one" — it
claims no fix for any board. Per the issue, the copy was **not** widened to say
these boards support only affected CPUs; B450 also runs Ryzen 1000/2000, which
sit outside the Ryzen 3000–9000 range the bulletin names.

**`npm test` and `npm run validate` green**

```
# tests 403
# pass 403
# fail 0

  NEW (fail build) : 0
  KNOWN (baseline) : 85
  RESOLVED         : 0
PASSED: no new violations.
```

Includes `scripts/gigabyte-b450m-d3hp.test.mjs`, which the issue asked to be
re-run: the two spec-only pages still make no benchmark or hands-on claim, quote
no price, award no star rating and carry no Amazon link.

`validation-baseline.json` is unchanged — `RESOLVED: 0`, so there was nothing to
shrink and no baseline refresh was needed.

## Note on the test run

`npm test` with no arguments reports 7 failures in
`scripts/tmp-issue-44-affiliate.test.mjs`, `tmp-issue-48-*` and `tmp-issue-49-*`.
Those files are **untracked scratch files** from other branches
(`git status` → `??`), not part of the repo, and they fail on `main` too. The
403/403 run above is the tracked suite — `git ls-files 'scripts/*.test.mjs'` —
which is what CI sees. No tracked test fails.

## Diff shape

Pure additions on the content pages, no deletions:

```
8  0  review-asus-rog-crosshair-viii-dark-hero.html
8  0  review-gigabyte-b450m-d3hp-wifi6e.html
8  0  review-gigabyte-b450m-d3hp.html
7  4  scripts/ftpm-advisory.test.mjs
```

No affiliate tag was added, removed or edited. The Dark Hero page still has its
3 `tag=motherboardcentral.com-20` occurrences; the two B450 pages still have 0.
The block sits above the first `</table>` on every page, outside the validator's
spec-contradiction window.

🤖 Generated with [Claude Code](https://claude.com/claude-code)
