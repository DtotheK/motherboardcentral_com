Refs #20 (does **not** close it — the bulk rollout PR closes it.)

Task 2 of the approved plan on #20: the copy-approval PR. One page, so the wording can be reviewed properly before it goes onto the other 34.

## Changed page

- https://motherboardcentral.com/review-msi-mag-b550-tomahawk.html

## What this adds

The AMD fTPM advisory block (`id="ftpm-advisory"`), byte-identical to the block agreed in the plan, with the `msi` vendor substitution applied. Placed immediately before `<h2 id="specifications">`, above the spec table.

## The two things to eyeball

- **Decision 6 — the vendor link.** This page links https://csr.msi.com/global/product-security-advisories, the MSI URL from the issue body. None of the four vendor URLs could be fetched from here (MSI and GIGABYTE 403 our fetcher, ASRock returns an empty body, ASUS renders its advisory table client-side). The copy is therefore written to claim nothing about what is on that page — the link text is "MSI's security advisories", not "MSI's fix for this board". **Please click it before the bulk PR goes out.**
- **Decision 8 — the date stamp.** The block reads "Advisory added 2026-08-20", the date this PR was actually authored, not the 2026-08-19 in the issue example (nothing shipped that day). Per CLAUDE.md rule 5. The same string will be pinned across all 35 pages and asserted exactly by the regression test in the bulk PR.

## Constraints held

- `css/style.css` **not touched**. Reuses the existing plain `.info-box` — no review page used the bare variant, so it collides with neither the Pros box (`info-box tip`) nor the Cons box (`info-box warning`).
- No AGESA or BIOS version string anywhere (published strings conflict and none is verified per board — CLAUDE.md rule 1).
- No `AM4`/`AM5` token inside the block, and no claim that a fixed BIOS exists for this board.
- `<title>`, meta description and canonical URL unchanged.
- No nav, footer or TOC markup touched.

## Verification

- `npm run validate` → `PASSED`, `NEW (fail build): 0`, `RESOLVED: 0`
- `npm test` → 274 tests, 0 fail
- Block ordering confirmed: advisory at line 137, `<h2 id="specifications">` at 145, first `</table>` at 164 — so the block sits outside the validator's spec-contradiction window.

## Next

Task 3 of the plan (the remaining 34 pages) and Task 4 (the regression test asserting all 35) are **blocked on approval of this copy**, per the plan's explicit stop gate. If any wording changes here, the changed text is what gets propagated.

Labelled `needs-human`. Not auto-merged.
