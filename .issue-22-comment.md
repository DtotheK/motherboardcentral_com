## Duplicate of #20 — no separate work needed

Agent run, 21 August 2026. Picked this up as the oldest open `agent-ok` issue and stopped before implementing, because the work is already in flight elsewhere.

**#22 and #20 specify the same change.** Both ask for an AMD fTPM BIOS advisory notice (AMD-SB-7064, CVE-2026-6726 / CVE-2026-6727) on the same 35 AM4/AM5 review pages. Same CVE IDs, same disclosure date, same page list.

**Current state of that work:**

- PR #76 (merged) — advisory added to `review-msi-mag-b550-tomahawk.html` as the copy-approval pilot.
- PR #82 (open) — "Add the AMD fTPM BIOS advisory to the remaining 34 AMD review pages".
- Issue #83 (open) — follow-up for 3 AM4 boards that #20's filename filter missed.
- Issue #89 (open) — follow-up on the Intel scoping of the same caveat in `guide-tpm-secure-boot.html`.

Verified on main at this run:

```
$ grep -lE 'fTPM|AMD-SB-7064' *.html
review-msi-mag-b550-tomahawk.html
guide-tpm-secure-boot.html
```

That matches the state above: pilot merged, the other 34 still pending in PR #82.

**Why no PR from this run.** Opening a second branch against the same 35 files would conflict directly with PR #82 and risk two divergent versions of the same advisory copy — exactly the drift that CLAUDE.md rule 1 exists to prevent.

The planning comment on this issue (19 August) is still worth keeping — its decisions on omitting CVSS scores, severity adjectives and AGESA version numbers are the more conservative reading of the sources, and reviewers of PR #82 may want to check the merged copy against them.

**Suggested disposition:** close as duplicate of #20, or keep open purely as the tracking issue for that planning decision record. Labelling `needs-human` rather than closing, since close-out requires human approval.
