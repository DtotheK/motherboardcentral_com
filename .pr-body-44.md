Closes #44
Part of #4

Converts this batch's Amazon search URLs (`/s?k=`) to direct product links
(`/dp/<ASIN>`). Four of the five boards passed all four verification tests and
were converted. One did not and was left untouched.

## Converted (4)

| Board | ASIN | Verified title on the live page |
|---|---|---|
| ASUS ROG Maximus Z890 Hero | `B0DGWWRTPV` | ASUS ROG Maximus Z890 Hero Intel® Z890 LGA 1851 ATX Motherboard, Advanced AI PC-Ready, 22+2+1+2 Stages, DDR5, WiFi 7, 3X PCIe® 5.0 M.2, Thunderbolt™ 4, USB Type-C®, AI Overclocking/Cooling/Networking |
| ASUS ROG Strix Z890-E Gaming WiFi | `B0DJDFY3FL` | ASUS ROG Strix Z890-E Gaming WiFi - Intel Z890, LGA1851, DDR5 8800, DP, HDMI, 2xUSB4, 7xM.2, USB3.2, WiFi 7+BT, Aura RGB, 5Gb LAN, ATX, 18+1+2+2 Power, TB4, NitroPath DRAM, NPU Boost Motherboard |
| GIGABYTE Z890 AORUS Master | `B0DK7JMBBX` | GIGABYTE Z890 AORUS Master Motherboard - Supports Intel Core Ultra (Series 2) CPUs, 18+1+2 Phases VRM, up to 9500MHz DDR5 (OC), 2xPCIe 5.0 + 3xPCIe 4.0, Wi-Fi 7, 10GbE LAN, Thunderbolt 4 |
| MSI MEG Z890 ACE | `B0DJPTRP57` | MSI MEG Z890 ACE Gaming Motherboard (Support Core Ultra Series 2 Intel Processors, LGA 1851, DDR5, PCIe 5.0, M.2, SATA, 10Gbps LAN, USB Type-C, Wi-Fi 7, Bluetooth 5.4, Thunderbolt 4, ATX) |

All four agree with our spec tables on socket (LGA 1851), chipset (Z890),
memory generation (DDR5), form factor (ATX) and WiFi variant (WiFi 7). All four
listings are new and bare — not Renewed, not a bundle.

## UNVERIFIED (1)

| Board | Reason |
|---|---|
| ASRock Z890 Taichi | The only exact-name search hit, `B0DJRNZWGN`, no longer resolves to this board. A live fetch of `/dp/B0DJRNZWGN` — retried once via the long SEO URL — returns "ASRock Intel Core Ultra Z890 Lightning WiFi 7 Phantom Gaming 9066 MHz DDR5 256 GB ATX Motherboard M.2 LGA1851RL-ILM", so test (a) exact model-name match fails. A control fetch of `B0DJRPLRJB` returned its own correct title (Z890 Taichi OCF), which rules out a fetcher fault. A re-query without the `site:` filter returned the same ASIN. The remaining Taichi listings are `OCF`, `Lite` and `Aqua` — all rejected as revision-token mismatches. Its links are untouched. |

**PENDING-HUMAN: none.**

`B0DJRNZWGN` is not recorded in the ledger, because the board was not converted.

## Changed pages

- https://motherboardcentral.com/review-asus-rog-maximus-z890-hero.html
- https://motherboardcentral.com/review-asus-rog-strix-z890-e-gaming-wifi.html
- https://motherboardcentral.com/review-gigabyte-z890-aorus-master.html
- https://motherboardcentral.com/review-msi-meg-z890-ace.html
- https://motherboardcentral.com/reviews.html
- https://motherboardcentral.com/best-motherboard-for-core-ultra-9-285k.html

Plus `docs/affiliate-asins.md` and `validation-baseline.json`. **8 files total** —
7 under the 15-file tripwire.

The ledger already existed (created by a later batch that merged first) and
already carried the four pilot #3 backfill rows, so batch 1's "create the
ledger" extra reduced to appending this batch's four rows plus the UNVERIFIED
note.

## Acceptance criteria — evidence

**Zero remaining search URLs for every converted board**

```
$ grep -rn 's?k=' --include=*.html . | grep -icE 'Maximus\+Z890\+Hero|Strix\+Z890-E|GIGABYTE\+Z890\+AORUS\+Master|MSI\+MEG\+Z890\+ACE'
0
```

**Every new URL matches the required format**

```
$ grep -roh 'https://www\.amazon\.com/dp/[^"]*' --include=*.html . | sort -u \
    | grep -cvE '^https://www\.amazon\.com/dp/[A-Z0-9]{10}\?tag=motherboardcentral\.com-20$'
0
```

**Tag count invariant — 341 before, 341 after**

```
$ grep -roh 'tag=motherboardcentral.com-20' --include=*.html . | wc -l
341
```

**`git diff -U0` touches only Amazon `href` values**

```
$ git diff -U0 -- '*.html' | grep -E '^[+-]' | grep -v '^[+-][+-]' \
    | grep -cvE 'href="https://www\.amazon\.com/(s\?k=|dp/)'
0
```

No spec table, card markup, nav, footer or layout markup was touched. No `rel`
attribute changed. No existing affiliate tag was edited or removed. No prices or
availability copied from Amazon.

**Validator green, `NEW = 0` and `RESOLVED = 0` after the baseline refresh**

```
$ npm run validate
  Total violations : 87 unique (153 occurrences)
  NEW (fail build) : 0
  KNOWN (baseline) : 87
  RESOLVED         : 0
PASSED: no new violations. 87 known issue(s) still outstanding.
```

**Baseline drops by exactly the 11 (page, URL) pairs converted, affiliate-only**

98 → 87. The pre-refresh run listed exactly those 11 as `RESOLVED`:

```
$ grep -c 'affiliate-search-url' validation-baseline.json
87
```

**Tests**

Acceptance criteria were encoded as a test first and watched fail before the
edit (4 of 9 failing: conversions absent, ledger rows absent, baseline unshrunk),
then pass after:

```
$ npm test -- scripts/tmp-issue-44-affiliate.test.mjs     # before the edit
# tests 9 / # pass 5 / # fail 4
$ npm test -- scripts/tmp-issue-44-affiliate.test.mjs     # after
# tests 9 / # pass 9 / # fail 0
```

The tracked suite plus this batch's checks:

```
$ npm test -- <the 14 tracked scripts/*.test.mjs> scripts/tmp-issue-44-affiliate.test.mjs
# tests 377 / # pass 377 / # fail 0
```

A bare `npm test` reports 3 failures, all pre-existing and unrelated: they come
from `scripts/tmp-issue-48-affiliate.test.mjs` and
`scripts/tmp-issue-49-affiliate.test.mjs`, untracked scratch files left in the
working tree by earlier merged batches that hard-code stale tag counts. Neither
file appears in `git ls-files`; both failed identically on `main` before this
change. This branch's scratch test is likewise left untracked, matching the
convention those batches set.

## Spec observations (not changed, per the issue's "do not edit any spec table")

Two listings disagree with our LAN values. LAN is not in the recipe's comparison
set, so neither blocked conversion, but both are worth a look:

- **ASUS ROG Strix Z890-E Gaming WiFi** — listing says `5Gb LAN`, our table says `2.5G`.
- **MSI MEG Z890 ACE** — listing says `10Gbps LAN`, our table says `5G+2.5G`.

🤖 Generated with [Claude Code](https://claude.com/claude-code)
