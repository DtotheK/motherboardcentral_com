Tasks 3–6 of the approved plan are done. PR #82 — https://github.com/DtotheK/motherboardcentral_com/pull/82 — labelled `needs-human`, **auto-merge not enabled**.

## Shipped

34 pages get the block approved in #76, plus `scripts/ftpm-advisory.test.mjs`. With the page from #76 that is all 35 the issue names, each carrying it exactly once.

Follow-ups filed as the plan's Task 6:

- #83 — the 3 AM4 boards the filename filter misses (`agent-ok`). Confirmed: selecting by spec table, `grep -lE '<td>Socket</td>[[:space:]]*<td>AM[45]' review-*.html`, returns 38 pages, i.e. the 35 plus `review-asus-rog-crosshair-viii-dark-hero.html`, `review-gigabyte-b450m-d3hp.html`, `review-gigabyte-b450m-d3hp-wifi6e.html`.
- #84 — the site-wide Updated-date convention (`needs-plan`), which is decision 9's re-scoping of one acceptance criterion.

## Evidence

| Check | Command | Result |
|---|---|---|
| Block on 35 pages, once each | `grep -c 'ftpm-advisory' review-*.html \| grep -v ':0$'` | 35 lines, all `:1` |
| Byte-identical block | `grep -h -A7 'id="ftpm-advisory"' review-*.html \| sort \| uniq -c` | shared lines ×35; vendor line 12 ASUS / 11 MSI / 8 GIGABYTE / 4 ASRock |
| No cross-brand vendor link | 4 × `grep -l '<vendor url>' review-*.html \| grep -v '^review-<brand>-'` | no output, all four |
| Validator | `npm run validate` | `PASSED` — `NEW (fail build): 0`, `KNOWN: 98`, `RESOLVED: 0` |
| Advisory tests | `npm test` | all 6 pass |
| Diff scope | `git diff --stat main...task/issue-20` | 35 files, 369 insertions, **0 deletions** |
| Shared assets | `git diff main...task/issue-20 -- css/ js/ index.html reviews.html sitemap.xml package.json validation-baseline.json` | empty |

Red-green was observed, not assumed: the test was committed before the bulk edit and 4 of its 6 cases failed against that tree; the edit turned them green.

`npm test` also reports 3 failures that are not from this branch — `tag count across the HTML corpus is unchanged`, `baseline shrinks by exactly the pairs converted...` and `tag count across the repo is unchanged`. All three live in untracked `scripts/tmp-issue-48-affiliate.test.mjs` / `scripts/tmp-issue-49-affiliate.test.mjs` left by other branches, and they failed identically before the first edit here.

## Acceptance criteria

| Issue AC | Status |
|---|---|
| Advisory block on each of the 35 pages, house tone | Done — test 2 |
| CVE IDs, Ryzen 3000–9000, BIOS-update fix, check your own board | Done — test 3 |
| BitLocker recovery-key warning | Done — bolded, test 3 |
| Dated so it does not read as open-ended | Done — `Advisory added 2026-08-20`, exact-string assertion |
| Vendor link, general site since no per-board page exists | Done — test 4 |
| No unverified BIOS version linked | Done — test 6 asserts no AGESA string |
| Bump the page "Updated" date | **Re-scoped per approved decision 9** — no review page has such a date; filed as #84 |
| `npm run validate` green | Done |

## Before merging

The two things the plan asked for your eyes on still stand: click the four vendor URLs (none was verifiable from here — MSI/GIGABYTE 403, ASRock empty body, ASUS renders client-side), and confirm the `2026-08-20` stamp still reads honestly.

The merge of #82 closes this issue.
