/* Spent research helper for issue #31 -- it extracted the MSI BIOS user guide
 * PDF so the MSI.ROM rename rule could be checked at MSI's own source. Kept as
 * a no-op stub because this session cannot delete files; it is untracked and
 * must never be committed. */

import { test } from 'node:test';

test.skip('tmp pdf extractor (spent)', () => {});
