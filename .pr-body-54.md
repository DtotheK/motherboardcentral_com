Closes #54
Part of #4

Converts Amazon search URLs (`/s?k=`) to direct product links (`/dp/<ASIN>`)
for the four AM4 legacy boards that passed all four verification tests.
**Two boards were deliberately left untouched** — see PENDING-HUMAN below.

## Changed pages

- https://motherboardcentral.com/review-asrock-b550m-steel-legend.html
- https://motherboardcentral.com/review-asus-rog-strix-b550-f-gaming-wifi-ii.html
- https://motherboardcentral.com/review-gigabyte-b550-aorus-elite-v2.html
- https://motherboardcentral.com/review-msi-mag-b550-tomahawk.html
- https://motherboardcentral.com/best-motherboard-for-ryzen-5-3600.html
- https://motherboardcentral.com/best-motherboard-for-ryzen-5-5600x.html
- https://motherboardcentral.com/best-motherboard-for-ryzen-7-3700x.html
- https://motherboardcentral.com/reviews.html

Plus `validation-baseline.json` and `docs/affiliate-asins.md` — **10 files total**,
5 under the 15-file tripwire.

## Board → ASIN

| Board | ASIN | Listing title (verbatim) | Occurrences |
|---|---|---|---|
| ASRock B550M Steel Legend | `B089W2Q2QC` | ASRock B550M Steel Legend Supports 3rd Gen AMD AM4 Ryzen™ / Future AMD Ryzen™ Processors Motherboard | 7 |
| ASUS ROG Strix B550-F Gaming WiFi II | `B09GP7P1XS` | Asus ROG Strix B550-F Gaming WiFi II AMD AM4 (3rd Gen Ryzen) ATX Gaming Motherboard (PCIe 4.0,WiFi 6E, 2.5Gb LAN, BIOS Flashback, HDMI 2.1, Addressable Gen 2 RGB Header and Aura Sync) | 7 |
| GIGABYTE B550 AORUS Elite V2 | `B08JHDL4WP` | Gigabyte B550 AORUS Elite V2 (AMD Ryzen 5000/B550/ATX/PCIe4.0/DDR4/USB3.2 Gen 1/Realtek ALC1200/M.2/2.5 GbE LAN/HDMI/DP/Gaming Motherboard) | 7 |
| MSI MAG B550 Tomahawk | `B089CWDHFZ` | MSI MAG B550 Tomahawk Gaming Motherboard (AMD Ryzen 5000 Series, AM4, DDR4, PCIe 4.0, SATA 6Gb/s, M.2, USB 3.2 Gen 2, HDMI/DP, 2.5Gbps LAN, ATX) | 7 |

Each fetched page agrees with our spec table on socket (AM4), chipset (B550),
memory generation (DDR4) and form factor, and each listing is new and bare —
not Renewed, not a bundle.

## UNVERIFIED

None.

## PENDING-HUMAN (2 boards — links left untouched, spec tables not edited)

| Board | Candidate ASIN | Discrepancy |
|---|---|---|
| ASUS ROG Crosshair VIII Dark Hero | `B08MTKJ6HM` | Our spec table says chipset **X570**; the genuine listing is titled **X570S**. Flagged in advance by #54. |
| MSI MEG X570S Unify-X Max | `B09GLBW5LL` | Our spec table says chipset **X570**; the live listing reports chipset **X570S**. |

The MSI board hit the *same* test-(c) failure #54 anticipated only for the Dark
Hero, so recipe step 6 applies to it identically: do not use the ASIN, do not
edit the spec table. Note our own pages are internally inconsistent here — the
`<h1>` reads "MSI MEG X570**S** Unify-X Max" while the chipset cell and the
`data-chipset` attribute on `reviews.html` both read `X570`. Whether these pages
should say X570 or X570S is spec work against the manufacturer's page, not a
link conversion. Neither ASIN was written to the ledger.

## Acceptance criteria — evidence

- [x] **Zero residual `s?k=` for converted boards.** `grep -rn "s?k=" --include=*.html .` filtered to the four converted model names returns **0**. The two PENDING-HUMAN boards are named above and keep their search URLs by design.
- [x] **URL format.** All 28 new URLs match `^https://www\.amazon\.com/dp/[A-Z0-9]{10}\?tag=motherboardcentral\.com-20$`; a grep for any `/dp/` URL *not* matching that pattern returns nothing.
- [x] **Tag count invariant.** `tag=motherboardcentral.com-20` occurs **336** times before and **336** times after.
- [x] **`npm test` exits 0** — 38/38 pass, 0 fail. **`npm run validate` exits 0** with `NEW = 0` and `RESOLVED = 0` after the baseline refresh.
- [x] **Baseline drop is exact.** `count` 159 → 139 = **20**, equal to the 20 (page, URL) pairs converted (4 boards × 5 pages). All 20 removed entries are `affiliate-search-url`; the only added line is the new `count`. All 139 remaining baseline entries are `affiliate-search-url` — no other rule appears.
- [x] **Diff scope.** `git diff -U0 -- '*.html'` shows 28 changed lines and **every one** is inside an `href="https://www.amazon.com…"` attribute; filtering those out leaves zero lines. No card markup, nav, footer, layout, prose, `rel` attribute or spec table was touched, and no existing affiliate tag was edited or removed.
- [x] **Ledger integrity.** 17 rows, 17 unique ASINs, no duplicates; each of the four new ASINs appears exactly once and maps to exactly one board.

## Not auto-merging

Per recipe step 12 and the CLAUDE.md tripwire ("uncertain whether a spec is
correct"), this batch carries two PENDING-HUMAN spec discrepancies, so it is
labelled `needs-human` and auto-merge is **not** enabled. #54 anticipated this.

No prices, availability or stock claims were copied from Amazon into any page.

🤖 Generated with [Claude Code](https://claude.com/claude-code)
