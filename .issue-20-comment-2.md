Executing the approved plan. **Task 1 and Task 2 are done; stopped at the plan's Task 2 Step 8 gate.**

## Shipped

PR #76 — https://github.com/DtotheK/motherboardcentral_com/pull/76 — one page, labelled `needs-human`, not auto-merged, body says `Refs #20` (not `Closes`).

- https://motherboardcentral.com/review-msi-mag-b550-tomahawk.html

The block is byte-identical to the one in the approved plan, with the `msi` row of the substitution table applied. Nothing else in the plan was changed.

## Evidence

| Check | Command | Result |
|---|---|---|
| Scope still 35 | `ls -1 review-*.html \| grep -Ei 'b550\|x570\|a620\|b650\|x670\|x870' \| wc -l` | `35` |
| Block placed above the spec table | `grep -n 'ftpm-advisory\|<h2 id="specifications">\|</table>'` | advisory `137` < heading `145` < `</table>` `164` |
| Exactly one block | `grep -c 'ftpm-advisory' review-msi-mag-b550-tomahawk.html` | `1` |
| No AGESA string | `grep -n 'AGESA\|ComboAM' review-msi-mag-b550-tomahawk.html` | no output |
| Validator | `npm run validate` | `PASSED` — `NEW (fail build): 0`, `KNOWN: 127`, `RESOLVED: 0` |
| Test suite | `npm test` | 274 tests, **0 fail**, 2 skipped |
| Diff scope | `git diff --stat main...task/issue-20` | 1 file, +8 lines |

`css/style.css` untouched. No nav, footer, TOC, `<title>`, meta description or canonical changed.

## Why this run stopped here

Plan Task 2 Step 8: *"**STOP.** Do not start Task 3 until a human approves the copy on that PR."* The `plan approved` above approves the plan including that gate, so I have not touched the other 34 pages.

## To unblock the rest

Read the block on PR #76 and either approve it or edit the wording there — whatever the text says after your edit is what Task 3 propagates. The two things the plan flagged for your eyes:

1. **Click the four vendor URLs.** None was verifiable from here (MSI/GIGABYTE 403, ASRock empty body, ASUS renders its advisory table client-side). The copy claims nothing about their contents, but a dead link would still be a dead link.
2. **The date stamp reads `Advisory added 2026-08-20`**, not the `2026-08-19` in the issue example — that was the filing date, and nothing shipped that day. Rule 5.

Then a follow-up run does Task 3 (remaining 34 pages), Task 4 (`scripts/ftpm-advisory.test.mjs`), Task 5 (bulk PR, `Closes #20`, `needs-human`) and Task 6 (the two follow-up issues). Branch `task/issue-20` is already pushed and the bulk work continues on it.

Leaving #20 open and `agent-ok`.
