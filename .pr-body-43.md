Closes #43

## What changed

Two review cards on the homepage drew a different star count than the same boards' own review pages. Both now follow the sitewide rule.

| Board | Rating | index.html before | index.html after | Review page |
|---|---|---|---|---|
| MSI MAG B760 Tomahawk WiFi | 4.6/5 | 4 filled + 1 outline | **5 filled** | 5 filled |
| ASUS ROG Strix X670E-E Gaming WiFi | 4.7/5 | 4 filled + 1 outline | **5 filled** | 5 filled |

Two lines in `index.html`. Numeric ratings untouched. No other page modified.

## Changed pages

- https://motherboardcentral.com/index.html

Reference only, **not** modified:
- https://motherboardcentral.com/review-msi-mag-b760-tomahawk-wifi.html
- https://motherboardcentral.com/review-asus-rog-strix-x670e-e-gaming-wifi.html
- https://motherboardcentral.com/reviews.html

## The rule, verified rather than assumed

Before writing the fix I tallied the star glyphs against the rating on all 70 `review-*.html` pages. The rule holds without exception — 35 pages at five filled, 35 at four plus outline, cutoff exactly at 4.6:

```
12  ★★★★☆  4.5      12  ★★★★★  4.7
12  ★★★★☆  4.4      12  ★★★★★  4.6
 5  ★★★★☆  4.3       9  ★★★★★  4.8
 2  ★★★★☆  4.1       2  ★★★★★  4.9
 2  ★★★★☆  4.0
 1  ★★★★☆  4.2
 1  ★★★★☆  3.9
```

## Acceptance criteria

| # | Criterion | Evidence |
|---|---|---|
| 1 | Both cards render `&#9733;` ×5 | `grep -n` on `index.html` → lines 209 and 255 now five filled; line 232 (4.5) correctly still four + outline |
| 2 | Numeric ratings unchanged | Diff touches only the `stars` span; test `the numeric ratings on the homepage are unchanged` asserts 4.8 / 4.6 / 4.5 / 4.7 |
| 3 | index.html only | `git diff --stat` → `index.html \| 4 ++--`, 1 file changed |
| 4 | No board differs between homepage and its review page | Test `every homepage review card draws the same stars as its own review page` — passes for all 4 cards |
| 5 | `npm run validate` passes | `PASSED: no new violations. 98 known issue(s) still outstanding.` — 0 new, 0 resolved, so no baseline change |

## Tests

`scripts/homepage-star-ratings.test.mjs` (new) checks every homepage review card against both the sitewide `>= 4.6` rule and the star count on its own review page.

Red-green verified: before the fix it failed with

```
MSI MAG B760 Tomahawk WiFi (4.6/5) draws 4 filled stars on index.html
MSI MAG B760 Tomahawk WiFi: index.html draws 4 filled stars,
  review-msi-mag-b760-tomahawk-wifi.html draws 5
```

After the fix, 4/4 pass. Full tracked suite: **368/368 pass**.

> `npm test` also picks up three failures in untracked `scripts/tmp-issue-48-affiliate.test.mjs` and `scripts/tmp-issue-49-affiliate.test.mjs` — scratch files from other branches sitting in the working tree, unrelated to this change and not part of this PR.

## One finding, deliberately left alone

While verifying criterion 4 I found a **third** disagreement that issue #43 does not mention and its scope rules out fixing here:

- **GIGABYTE B650 AORUS Elite AX (4.5)** — `reviews.html` draws **five** filled stars, but `index.html` and `review-gigabyte-b650-aorus-elite-ax.html` both correctly draw four + outline.

So `reviews.html` violates the sitewide rule on that one card. The issue states the rule "holds ... on reviews.html", which is not quite right. I did not touch it: acceptance criterion 3 says "No other page is modified — this is index.html only," and criterion 4 is scoped to homepage-vs-review-page, which this is not. The new test documents the exclusion in a comment rather than silently ignoring it. Worth a one-line follow-up issue.

🤖 Generated with [Claude Code](https://claude.com/claude-code)
