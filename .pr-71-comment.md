Labelled `needs-human` and **not** enabling auto-merge, for one reason only: acceptance criterion 2 asks for every spec to be verified against the GIGABYTE product page, and `gigabyte.com` returns HTTP 403 to automated retrieval, so I could not open it directly. I read the values off that exact page via search-engine extraction and corroborated each against an independent outlet naming the same SKU — details in the PR body — but that is one step short of what the criterion asks for.

The 15-file tripwire is not in play: 5 files, and no nav, footer, layout or shared template markup is touched.

**The 30-second check, if you want to clear this:** open https://www.gigabyte.com/Motherboard/B760M-GAMING-WIFI6E-DDR4-GEN5/sp in a browser and confirm six values against the spec table on the new page:

- 2 x DDR4 DIMM, **up to 64GB** — this is the one value I derived rather than read (GIGABYTE lists 32GB single-DIMM capacity on its 2-DIMM boards; 2 x 32 = 64). Everything else was read directly.
- 1 x PCIe 5.0 x16 and 1 x PCIe 3.0 x1
- 2 x M.2, **both PCIe 4.0 x4** (not Gen5)
- Hybrid **6+2+1** phases (the GAMING X is 8+1+1 — that model's spec sheet kept surfacing in searches and is the main contamination risk here)
- 4 x SATA 6 Gb/s, 2.5GbE, Wi-Fi 6E
- 8-channel HD audio

If all six match, this is good to merge as-is. If any differ, the failing value is in the spec table and in `SPECS` at the top of `scripts/gigabyte-b760m-gaming-wifi6e-ddr4-gen5.test.mjs`, so correcting it there will fail the build until the page agrees.

Everything I could not attribute to this exact SKU is deliberately absent from the page and listed in its "What we have not verified" section: rear panel layout, video outputs, audio codec, Bluetooth version, memory speed list, price. No Amazon link — no ASIN could be confirmed as this model rather than one of its four near-identically-named siblings.

`npm test` 141/141 pass. `npm run validate` 0 new violations across 99 pages.
