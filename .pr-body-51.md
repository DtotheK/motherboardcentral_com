Closes #51
Part of #4

Converts this batch's Amazon `/s?k=` search URLs to direct `/dp/<ASIN>` product links. `href` values only — no markup, nav, footer, layout, text, `rel` or spec-table changes.

## Board → ASIN

| Board | ASIN | Verified |
|---|---|---|
| ASUS ROG Crosshair X670E Hero | `B0BDTN8SNJ` | yes — all four tests |
| GIGABYTE X670E AORUS Master | `B0BFNVND8B` | yes — all four tests |
| MSI MEG X670E ACE | `B0B6Q4X5NF` | yes — all four tests |
| MSI MPG X670E Carbon WiFi | `B0B6Q23SPC` | yes — all four tests |
| ASRock X670E Taichi | — | **UNVERIFIED**, links left untouched |

Each verified board: exact model-name match against our `<h1>` and spec table; live fetch of `https://www.amazon.com/dp/<ASIN>` returned a real product page; socket (AM5), chipset (X670E) and memory generation (DDR5) agree with our spec table; listing is new and bare.

Rejected during selection:

- `B0BGPGH6WG` — ASRock X670E Taichi **Carrara**, distinct SKU.
- `B0C5W71Z4S` — Crosshair X670E Hero, **Renewed**.
- `B0C23VB9L3`, `B0BQW3F727`, `B0BM93C1C6`, `B0BV3BFL41`, `B0BV3DZ7MN`, `B0BWRJVTPV` — CPU **bundles**.
- `B0CFS6ZST7` — MPG X670E Carbon WiFi, **Renewed**.
- `B0CJ5T28Q9` — X670E AORUS Master, **Renewed**.
- `B0BF7CL99N` — search results labelled this "GIGABYTE X670E AORUS Master", but fetching it returns **GIGABYTE X870E Eagle X3D**. Wrong chipset, rejected. This is why the fetch step exists.
- `B0GC7DRGQG`, `B0G77DKBBK`, `B0G6X6HP42` — "Fit for …" clone listings.
- `B0B94XN488` (MEG X670E **GODLIKE**), `B0BF73P6HY` (X670E AORUS **Xtreme**), `B0CN76TH9S` (X670E AORUS **PRO X**), `B0CRF81BBC` (X670E **GAMING PLUS**) — different models.
- `us.amazon.com` host for `B0B6Q23SPC` — rejected host; the `www.amazon.com` listing was used.

## UNVERIFIED

**ASRock X670E Taichi** — the only genuine listing found is `B0BGPF7K2P`. Every fetch of it returned HTTP 500: `https://www.amazon.com/dp/B0BGPF7K2P`, the long SEO URL, and a third attempt. Other Amazon `/dp/` fetches in this same run succeeded, so this is not a blanket block. Test (b) could not pass, so per the recipe the board is UNVERIFIED and its search URLs are left in place. A re-query without `site:` surfaced no other genuine listing.

## PENDING-HUMAN

None — no board was rejected for a socket/chipset/memory-generation disagreement.

## Spec observations (no spec table was edited)

Two differences outside the three fields test (c) covers. Flagging rather than guessing, per CLAUDE.md rule 1. Both manufacturer spec pages (`msi.com`, `gigabyte.com`) returned HTTP 403, so neither could be settled against the vendor:

- **MSI MEG X670E ACE** — Amazon title says `EATX`; our spec table says `ATX`.
- **ASUS ROG Crosshair X670E Hero** — Amazon title says `5X M.2 Slots`; our spec table says `4x M.2`. Plausibly onboard-only vs. including the bundled expansion card, but unconfirmed.

## Changed pages

- https://motherboardcentral.com/review-asus-rog-crosshair-x670e-hero.html
- https://motherboardcentral.com/review-gigabyte-x670e-aorus-master.html
- https://motherboardcentral.com/review-msi-meg-x670e-ace.html
- https://motherboardcentral.com/review-msi-mpg-x670e-carbon-wifi.html
- https://motherboardcentral.com/reviews.html

Non-page files: `validation-baseline.json`, `docs/affiliate-asins.md` (created — it did not exist; seeded with the four pilot ASINs from #3 plus this batch). **7 files total**, under the 15-file tripwire.

## Acceptance criteria — evidence

- [x] **Zero residual `s?k=` for converted boards.** `grep -rn 's?k=' --include=*.html . | grep -i x670e` now yields exactly one URL spelling, `s?k=ASRock+X670E+Taichi+motherboard` — the named UNVERIFIED board.
- [x] **URL format.** All four new URLs match `^https://www\.amazon\.com/dp/[A-Z0-9]{10}\?tag=motherboardcentral\.com-20$`, 4 occurrences each (3 on the review page, 1 on `reviews.html`).
- [x] **Tag count invariant.** `tag=motherboardcentral.com-20` occurrences across the repo: **336 before, 336 after.**
- [x] **`npm test`** — 38 pass, 0 fail. **`npm run validate`** — `NEW = 0`, `RESOLVED = 0`, `PASSED`.
- [x] **Baseline.** `count` 181 → 173, a drop of exactly 8 — the 8 (page, URL) pairs converted. The only rule remaining in the baseline is `affiliate-search-url`.
- [x] **Diff shape.** `git diff --word-diff=porcelain -- '*.html'` shows only complete `href="…amazon…"` attributes on either side — 4 removed spellings, 4 added, nothing else.
- [x] **Ledger.** Each ASIN appears exactly once, mapped to exactly one board.

## Merge gate

Labelled `needs-human`, auto-merge **not** enabled: the batch has one UNVERIFIED board, and per the CLAUDE.md tripwire I am uncertain about the two spec-table values noted above.

🤖 Generated with [Claude Code](https://claude.com/claude-code)
