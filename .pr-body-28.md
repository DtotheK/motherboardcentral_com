Closes #28

Pre-release preview article for the **GIGABYTE B550 X Series** (Socket AM4, AMD B550), announced 31 July 2026. Not a review, not a board record, no affiliate link.

## Changed pages

- https://motherboardcentral.com/preview-gigabyte-b550-x-series.html — **new** preview article
- https://motherboardcentral.com/guides.html — new "News & Previews" section linking the preview
- `sitemap.xml` — added the preview URL (priority 0.7)
- `scripts/preview-b550-x-series.test.mjs` — **new** guard tests

## Acceptance criteria — evidence

| Criterion | Evidence |
|---|---|
| New preview page created, matching existing page structure | `preview-gigabyte-b550-x-series.html`, same nav / hero / `guide-layout` + sidebar TOC / footer / `js/main.js` shell as the review pages |
| Only claims traceable to the three sources | Every factual statement in "What GIGABYTE announced" maps 1:1 to the issue's confirmed list: date, series name, socket + chipset, the Gaming / Eagle / AORUS Elite families, ATX + Micro-ATX, PCIe EZ-Latch Plus, Ryzen 7 5800X3D positioning. All three sources linked in a Sources section. Direct fetches of all three URLs returned 403/402 to the agent; the confirmed facts were corroborated independently via web search (gigabyte.com press release title, TechPowerUp, VideoCardz, Vortez) before writing. |
| Explicitly labelled pre-release, above the fold | First element in the content column is a warning box: "Pre-release preview — announced, not released … have not yet been released, and we have not seen or tested one." Asserted by the test `flags itself as pre-release above the fold`, which checks the region **before the first `<h2>`** |
| No pricing, no release dates, no per-board specs, no invented SKU names | Tests `quotes no price`, `gives no release or on-sale date`, `publishes no per-board specification` (VRM phases, M.2 count, LAN speed, WiFi generation, memory speed, audio codec, SATA/USB counts), `names no individual board SKU beyond the three announced families`. The page also carries an explicit "What GIGABYTE has not said" section |
| No benchmarks or hands-on claims (rule 2) | Test `makes no benchmark or hands-on performance claim`; page states plainly that we have not seen or tested a board |
| Unique `<title>`, meta description, canonical (rule 4) | Present in the head; validator reports **0** `meta-duplicate` / `meta-missing` / `canonical-missing` findings across all 96 pages |
| Linked from where the site surfaces guides, and in `sitemap.xml` | Tests `is linked from the guides hub` and `is listed in sitemap.xml` |
| Affiliate links: none | Test `carries no Amazon link of any kind` |
| Honest dates (rule 5) | "Published 20 August 2026. Last updated 20 August 2026," with a stated commitment to move the date only when content changes |
| `npm run validate` green | `96 HTML pages scanned` · `NEW (fail build): 0` · `RESOLVED: 0` · `PASSED: no new violations` (159 known pre-existing affiliate-search-URL issues unchanged; baseline untouched) |

## Tests

`npm test` — **67/67 pass** (16 new).

The 16 new tests were written first and watched fail (page absent). Because a "file missing" failure does not prove a *content guard* works, each guard was then re-verified by injecting a deliberate violation (a fake SKU with a 12+2 VRM, 2x M.2, 2.5 Gigabit Ethernet, Wi-Fi 7, a $199 MSRP, an October 2026 ship date, a benchmark claim and an Amazon link) into the page and confirming that all six content guards fired:

```
not ok 19 - makes no benchmark or hands-on performance claim
not ok 20 - quotes no price
not ok 21 - gives no release or on-sale date
not ok 22 - publishes no per-board specification
not ok 23 - names no individual board SKU beyond the three announced families
not ok 24 - carries no Amazon link of any kind
```

The probe was then reverted and the suite returned to 67/67.

## Tripwire check

4 files changed. No shared template, nav, footer or layout markup was modified — the `guides.html` diff is a self-contained new `<section>` using existing classes, and the sitemap diff is one line. No spec uncertainty, because the page publishes no specs.

🤖 Generated with [Claude Code](https://claude.com/claude-code)
